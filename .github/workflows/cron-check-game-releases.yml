name: Cron check game releases
run-name: Cron release check smoke · Hello world

on:
  schedule:
    - cron: "*/5 * * * *"

permissions: {}

jobs:
  hello-world:
    name: Hello world
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Hello world
        run: echo "Hello world"
