# fissix syntax validator

- Pinned version: `24.4.24` (see `pyproject.toml` and `uv.lock`).
- Upstream: <https://github.com/amyreese/fissix>
- License: Python Software Foundation License Version 2.
- Integration: parses generated source as data with the Python 2 grammar; it never imports or
  executes game code.

The pure-Python package and its license metadata are included by `uv sync` in the container image.
The readable policy and per-file diagnostics record the exact validator version.
